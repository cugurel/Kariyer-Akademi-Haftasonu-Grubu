string identity = "";
int sum= 0;

//for(int i=0; i<=identity.Length-2; i++)
//{
//    sum = sum + Convert.ToInt32(identity[i].ToString());

//}


if(mod == 2)
{
    Console.Write("Tc Kimlik No geçerli");
}
else
{
    Console.Write("Tc Kimlik No geçersiz");
}